.menu-page { display: flex; height: 100vh; }
.left { width: 28%; height: 100vh; }
.right { flex: 1; padding: 10px; height: 100vh; }
.section-title { font-weight: 700; color: #6B4A2B; margin: 8px 0; }
.section { padding-bottom: 10px; }

cart-bar { position: fixed; left: 0; right: 0; bottom: 0; }
