建议在云开发控制台创建如下索引，以提升查询与排序性能：

- products 集合
  - 单键索引：`categoryId: 1`
  - 单键索引：`sort: -1`
  - 或者使用联合索引：`{ categoryId: 1, sort: -1 }`

说明：
- 列表页按 `categoryId` 过滤并按 `sort` 倒序排序，联合索引可覆盖该查询。
- 其余字段暂不需要索引，后续根据检索场景再补充。

