<scroll-view class="sidebar" scroll-y>
  <block wx:for="{{categories}}" wx:key="id">
    <view class="item {{currentId===item.id?'active':''}}" bindtap="onTap" data-id="{{item.id}}">
      {{item.name}}
    </view>
  </block>
  <view class="bottom-gap"></view>
</scroll-view>
